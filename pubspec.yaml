name: filepulse
description: 跨平台文件分析筛选工具 - FilePulse
version: 0.2.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.1
  path: ^1.8.3
  shared_preferences: ^2.2.2
  desktop_drop: ^0.5.0
  file_picker: ^8.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
